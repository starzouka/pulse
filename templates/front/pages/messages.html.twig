{% extends 'base.html.twig' %}

{% block title %}PULSE - Messagerie{% endblock %}

{% block body %}
  {% include 'front/partials/_hero_mini.html.twig' with {
    hero_kicker: 'JOUEUR',
    hero_title: 'Messagerie',
    hero_sub: 'Vue inbox style social pour toutes vos conversations.',
    breadcrumb_current: 'Messagerie'
  } %}

  <main class="page">
    <section class="belowHero">
      <div class="layout">
        {% include 'front/partials/_player_side_nav.html.twig' with { active: 'messages' } %}

        <div class="socialShell--chat">
          <aside class="socialStack">
            <section class="panel">
              <div class="filtersRow">
                <input class="input" type="search" placeholder="Rechercher une conversation..." />
              </div>
              <div class="socialQuickList" style="margin-top:10px;">
                <a class="listItem" href="{{ path('front_conversation') }}"><span>FrostByte</span><span class="badge">2 non lus</span></a>
                <a class="listItem" href="{{ path('front_conversation') }}"><span>NovaAim</span><span class="listItem__meta">Hier</span></a>
                <a class="listItem" href="{{ path('front_conversation') }}"><span>North Hydra</span><span class="listItem__meta">Lun</span></a>
              </div>
            </section>
          </aside>

          <div class="socialStack">
            <section class="panel">
              <div class="panel__head">
                <h3 class="panel__title">APERCU CONVERSATION</h3>
                <a class="btn btn--ghost" href="{{ path('front_conversation') }}">Ouvrir</a>
              </div>

              <div class="list">
                <div class="listItem"><span>FrostByte: Ready pour le scrim de ce soir ?</span><span class="listItem__meta">10:05</span></div>
                <div class="listItem"><span>Moi: Oui, je confirme 20h30.</span><span class="listItem__meta">10:06</span></div>
                <div class="listItem"><span>FrostByte: Parfait, on finalise la map pool.</span><span class="listItem__meta">10:08</span></div>
              </div>

              <div class="socialComposer" style="margin-top:10px;">
                <input class="input" type="text" placeholder="Ecrire un message..." />
                <button class="btn btn--primary" type="button">Envoyer</button>
              </div>
            </section>
          </div>
        </div>
      </div>

      {% include 'front/partials/_footer.html.twig' %}
    </section>
  </main>

  {% include 'front/partials/_auth_modal.html.twig' %}
{% endblock %}
