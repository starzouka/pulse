{% extends 'base.html.twig' %}

{% block title %}PULSE - Conversation{% endblock %}

{% block body %}
  {% include 'front/partials/_hero_mini.html.twig' with {
    hero_kicker: 'JOUEUR',
    hero_title: 'Conversation',
    hero_sub: 'Thread complet avec actions rapides.',
    breadcrumb_current: 'Conversation'
  } %}

  <main class="page">
    <section class="belowHero">
      <div class="layout">
        {% include 'front/partials/_player_side_nav.html.twig' with { active: 'conversation' } %}

        <div class="socialShell--chat">
          <aside class="socialStack">
            <section class="panel">
              <h3 class="panel__title">CONVERSATIONS</h3>
              <div class="socialQuickList">
                <a class="listItem" href="{{ path('front_conversation') }}"><span>FrostByte</span><span class="badge">Actif</span></a>
                <a class="listItem" href="{{ path('front_conversation') }}"><span>NovaAim</span><span class="listItem__meta">Dernier msg: hier</span></a>
                <a class="listItem" href="{{ path('front_conversation') }}"><span>Kairo</span><span class="listItem__meta">Dernier msg: lun</span></a>
              </div>
            </section>
          </aside>

          <div class="socialStack">
            <section class="panel">
              <div class="panel__head">
                <div>
                  <h3 class="panel__title">CHAT AVEC FROSTBYTE</h3>
                  <div class="panel__desc">Connecte maintenant</div>
                </div>
                <a class="btn btn--ghost" href="{{ path('front_player_profile') }}">Voir profil</a>
              </div>

              <div class="list">
                <div class="listItem"><span>FrostByte: GG pour hier !</span><span class="listItem__meta">10:05</span></div>
                <div class="listItem"><span>Moi: Merci, bien joue aussi.</span><span class="listItem__meta">10:06</span></div>
                <div class="listItem"><span>FrostByte: On planifie le prochain match samedi ?</span><span class="listItem__meta">10:07</span></div>
                <div class="listItem"><span>Moi: Oui, je reserve 20h.</span><span class="listItem__meta">10:08</span></div>
              </div>

              <div class="socialComposer" style="margin-top:10px;">
                <input class="input" type="text" placeholder="Ecrire un message..." />
                <button class="btn btn--primary" type="button">Envoyer</button>
              </div>
            </section>
          </div>
        </div>
      </div>

      {% include 'front/partials/_footer.html.twig' %}
    </section>
  </main>

  {% include 'front/partials/_auth_modal.html.twig' %}
{% endblock %}
